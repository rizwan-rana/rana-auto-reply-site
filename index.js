import React from 'react';
import ReactDOM from 'react-dom/client';
import ChatDashboard from './ChatDashboard';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<ChatDashboard />);
